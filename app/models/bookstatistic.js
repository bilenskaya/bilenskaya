/** * Module dependencies. */var mongoose = require('mongoose')  , env = process.env.NODE_ENV || 'development'  , config = require('../../config/config')[env]  , Schema = mongoose.Schema;/** * Bookstatistic Schema */var BookstatisticSchema = new Schema({    created: {type : Date, default : Date.now},    wordsTotal: {type: Number},    wordsUniqTotal: {type: Number},    wordsInSentenceMid: {type: Number},    levelTotal: {type: Number},    bookvocab: {},    book: {type: Schema.Types.ObjectId, ref: 'Book'},    user: {type : Schema.ObjectId, ref : 'User'}});/** * Statics */BookstatisticSchema.statics = {  load: function (id, cb) {    this.findOne({ _id : id }).populate('user book').exec(cb);  }};mongoose.model('Bookstatistic', BookstatisticSchema);